import {makeRace,venues} from '@/lib/mockData';import {predictRace,makeTrifectaPicks,aiConfidence} from '@/ai/predict';import EntryTable from '@/components/EntryTable';import PredictionTable from '@/components/PredictionTable';import ExpectedValueTable from '@/components/ExpectedValueTable';import ResultForm from '@/components/ResultForm';
export default function RacePage({params}:{params:{venueId:string;raceNo:string}}){const race=makeRace(params.venueId,Number(params.raceNo));const venue=venues.find(v=>v.id===params.venueId);const predictions=predictRace(race);const picks=makeTrifectaPicks(race,Number(params.raceNo));const conf=aiConfidence(picks);return <><div className="card"><div className="row"><div><h1>{venue?.name} {race.raceNo}R</h1><p className="muted">締切 {race.deadline} / {race.weather} / 風 {race.wind}m / 波 {race.wave}cm</p></div><div><div className="score">{conf.stars}</div><span className="pill">{conf.label}</span></div></div><p>{conf.message}</p></div><h2 className="section-title">出走表</h2><EntryTable race={race}/><div className="grid two"><div><h2 className="section-title">AI確率</h2><PredictionTable predictions={predictions}/></div><div><h2 className="section-title">期待値ランキング</h2><ExpectedValueTable picks={picks}/></div></div><h2 className="section-title">結果反映</h2><ResultForm raceId={race.id} bestCombo={picks[0]?.combo||''}/></>}
