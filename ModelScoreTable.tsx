'use client';
import {useState} from 'react';import {saveResult} from '@/services/storage';
export default function ResultForm({raceId,bestCombo}:{raceId:string;bestCombo:string}){const[res,setRes]=useState(bestCombo);const[payout,setPayout]=useState(0);const[bet,setBet]=useState(1000);const[done,setDone]=useState(false);function submit(){const hit=res===bestCombo;saveResult({raceId,result:res,payout:Number(payout),betAmount:Number(bet),hit,profit:(hit?Number(payout):0)-Number(bet)});setDone(true)}return <div className="card"><h3>結果入力</h3><p className="muted">推奨買い目：{bestCombo}</p><div className="grid"><label>確定3連単<input value={res} onChange={e=>setRes(e.target.value)}/></label><label>払戻金<input type="number" value={payout} onChange={e=>setPayout(Number(e.target.value))}/></label><label>投資額<input type="number" value={bet} onChange={e=>setBet(Number(e.target.value))}/></label><button className="btn" onClick={submit}>保存</button>{done&&<strong className="good">保存しました</strong>}</div></div>}
