import Link from 'next/link';import {Race} from '@/lib/types';
export default function RaceCard({race}:{race:Race}){return <div className="card"><div className="row"><h3>{race.raceNo}R</h3><span className="pill">{race.title}</span></div><p className="muted">締切 {race.deadline} / {race.weather} 風{race.wind}m 波{race.wave}cm</p><Link className="btn" href={`/race/${race.venueId}/${race.raceNo}`}>AI予想を見る</Link></div>}
