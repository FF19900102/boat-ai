import {TrifectaPick} from '@/types/boat';import {createPurchasePlan} from '@/ai/purchase';
export default function PurchasePlanPanel({picks}:{picks:TrifectaPick[]}){const plan=createPurchasePlan(picks);return <div className="card"><h3>購入プラン</h3><div className="row"><strong className={plan.judgement==='見送り'?'warn':'good'}>{plan.judgement}</strong><span className="pill">合計 {plan.total}円</span></div><p className="muted">{plan.message}</p><table className="table compact"><thead><tr><th>買い目</th><th>EV</th><th>金額</th></tr></thead><tbody>{plan.lines.map(l=><tr key={l.combo}><td>{l.combo}</td><td>{l.ev}</td><td>{l.amount}円</td></tr>)}</tbody></table></div>}
