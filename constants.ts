import {Race} from '@/types/boat';
export default function WeatherPanel({race}:{race:Race}){const rough=race.wind>=5||race.wave>=4;return <div className="card"><h3>水面・気象</h3><div className="grid split3"><div><p className="muted">天候</p><strong>{race.weather}</strong></div><div><p className="muted">風速</p><strong className={race.wind>=5?'warn':''}>{race.wind}m</strong></div><div><p className="muted">波高</p><strong className={race.wave>=4?'warn':''}>{race.wave}cm</strong></div></div><p className="mini" style={{marginTop:10}}>{rough?'荒れ要素あり。イン信頼度を少し下げて判定。':'水面は比較的安定。基本データ重視で判定。'}</p></div>}
