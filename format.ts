import{NextResponse}from'next/server';import{externalBoatRaceClient}from'@/services/externalBoatRaceClient';export async function GET(_:Request,{params}:{params:{raceId:string}}){const race=await externalBoatRaceClient.getRace(params.raceId);if(!race)return NextResponse.json({error:'not found'},{status:404});return NextResponse.json(race)}
