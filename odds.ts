import {Race} from '@/types/boat';import {dataCompleteness,validateRace} from '@/lib/validation';
export default function DataQualityPanel({race}:{race:Race}){const c=dataCompleteness(race);const issues=validateRace(race);return <div className="card"><h3>データ品質</h3><div className="row"><span className="muted">入力充足率</span><strong>{c}%</strong></div><div className="bar"><span style={{width:`${c}%`}}/></div><p className="mini">警告 {issues.filter(i=>i.level==='warn').length} / エラー {issues.filter(i=>i.level==='error').length}</p>{issues.slice(0,3).map((i,idx)=><div key={idx} className="reason"><strong>{i.field}</strong><br/><span className="mini">{i.message}</span></div>)}</div>}
