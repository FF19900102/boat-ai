import Link from "next/link";
import { Venue } from "@/lib/types";

export function VenueCard({ venue }: { venue: Venue }) {
  return (
    <Link href={`/race/${venue.id}`} className="card block p-5 hover:border-blue-400">
      <div className="text-xs font-bold text-blue-700">{venue.area}</div>
      <div className="mt-1 text-2xl font-black">{venue.name}</div>
      <div className="mt-3 text-sm text-gray-500">1R〜12Rを選択</div>
    </Link>
  );
}
