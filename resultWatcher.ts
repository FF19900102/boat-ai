import {Race,Prediction} from '@/types/boat';import {runModelLeague,selectSupervisorModel} from '@/ai/modelLeague';
export default function ModelLeaguePanel({race,predictions}:{race:Race;predictions:Prediction[]}){const league=runModelLeague(race);const sup=selectSupervisorModel(race,predictions);return <div className="card"><h3>AIリーグ</h3><p className="muted">AI監督採用：<strong>{sup.name}</strong></p><table className="table compact"><thead><tr><th>AI</th><th>本命</th><th>信頼度</th></tr></thead><tbody>{league.map(m=><tr key={m.id}><td>{m.name}<br/><span className="mini">{m.note}</span></td><td><span className={`lane lane${m.topLane}`}>{m.topLane}</span></td><td>{m.confidence}%</td></tr>)}</tbody></table></div>}
